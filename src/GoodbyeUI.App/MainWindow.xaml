<ui:FluentWindow x:Class="GoodbyeUI.App.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:tb="http://www.hardcodet.net/taskbar"
                 xmlns:views="clr-namespace:GoodbyeUI.App.Views"
                 Title="{Binding Loc[AppTitle]}"
                 Icon="pack://application:,,,/Assets/app.ico"
                 Width="960" Height="660"
                 MinWidth="760" MinHeight="540"
                 WindowStartupLocation="CenterScreen"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Sistem tepsisi ikonu ve sağ tık menüsü -->
        <tb:TaskbarIcon x:Name="TrayIcon"
                        IconSource="pack://application:,,,/Assets/app.ico"
                        ToolTipText="{Binding TrayToolTip}"
                        DoubleClickCommand="{Binding ShowCommand}">
            <tb:TaskbarIcon.ContextMenu>
                <!-- DataContext, MainWindow kurucusunda açıkça MainViewModel'e set edilir. -->
                <ContextMenu>
                    <!-- Mevcut durum (tıklanamaz başlık): Bağlandı / Bağlı değil / … -->
                    <MenuItem Header="{Binding StatusText}" IsEnabled="False" FontWeight="SemiBold" />
                    <Separator />
                    <MenuItem Header="{Binding Loc[Tray_Connect]}" Command="{Binding ConnectCommand}" />
                    <MenuItem Header="{Binding Loc[Tray_Disconnect]}" Command="{Binding DisconnectCommand}" />
                    <Separator />
                    <MenuItem Header="{Binding Loc[Tray_Open]}" Command="{Binding ShowCommand}" />
                    <MenuItem Header="{Binding Loc[Tray_Exit]}" Command="{Binding ExitCommand}" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>

        <ui:TitleBar Grid.Row="0"
                     Title="{Binding Loc[AppTitle]}"
                     ShowMaximize="True" />

        <ui:NavigationView x:Name="RootNavigation"
                           Grid.Row="1"
                           PaneDisplayMode="Left"
                           IsBackButtonVisible="Collapsed"
                           Margin="0,4,0,0">
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="{Binding Loc[Nav_Home]}"
                                       TargetPageType="{x:Type views:HomePage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Home24" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
                <ui:NavigationViewItem Content="{Binding Loc[Nav_Logs]}"
                                       TargetPageType="{x:Type views:LogsPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="History24" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
            </ui:NavigationView.MenuItems>

            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem Content="{Binding Loc[Nav_Settings]}"
                                       TargetPageType="{x:Type views:SettingsPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Settings24" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
                <ui:NavigationViewItem Content="{Binding Loc[Nav_About]}"
                                       TargetPageType="{x:Type views:AboutPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Info24" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
            </ui:NavigationView.FooterMenuItems>
        </ui:NavigationView>
    </Grid>
</ui:FluentWindow>
