<Page x:Class="GoodbyeUI.App.Views.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Background="Transparent">

    <Grid Margin="24,12,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Başlık + araçlar -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="{Binding Loc[Logs_Title]}" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="{Binding Loc[Logs_OpenFolder]}"
                           Icon="{ui:SymbolIcon FolderOpen24}"
                           Command="{Binding OpenFolderCommand}"
                           Margin="0,0,8,0" />
                <ui:Button Content="{Binding Loc[Logs_Clear]}"
                           Icon="{ui:SymbolIcon Delete24}"
                           Command="{Binding ClearCommand}" />
            </StackPanel>
        </Grid>

        <!-- Boş durum -->
        <TextBlock Grid.Row="1"
                   Text="{Binding Loc[Logs_Empty]}"
                   Opacity="0.6"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="{Binding HasEntries, Converter={StaticResource BoolToCollapsed}}" />

        <!-- Log listesi -->
        <Border Grid.Row="1"
                CornerRadius="8"
                Background="{DynamicResource ControlFillColorDefaultBrush}"
                Visibility="{Binding HasEntries, Converter={StaticResource BoolToVisible}}">
            <ListView ItemsSource="{Binding Entries}"
                      Background="Transparent"
                      BorderThickness="0"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      VirtualizingPanel.IsVirtualizing="True">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="8,4" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="{Binding Timestamp, StringFormat=HH:mm:ss}"
                                       Opacity="0.6"
                                       FontFamily="Consolas"
                                       Margin="0,0,12,0" />
                            <TextBlock Grid.Column="1"
                                       Text="{Binding Level}"
                                       FontFamily="Consolas"
                                       FontWeight="SemiBold"
                                       Width="64"
                                       Margin="0,0,12,0" />
                            <TextBlock Grid.Column="2"
                                       Text="{Binding Message}"
                                       FontFamily="Consolas"
                                       TextWrapping="Wrap" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>
